{% extends 'base.html.twig' %}

{% block title %}Production{% endblock %}

{% block body %}
    <h1>Production</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ production.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ production.contenu }}</td>
            </tr>
            <tr>
                <th>PrixFinal</th>
                <td>{{ production.prixFinal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_production_index') }}">back to list</a>

    <a href="{{ path('admin_production_edit', {'id': production.id}) }}">edit</a>

    {{ include('admin_production/_delete_form.html.twig') }}
{% endblock %}
