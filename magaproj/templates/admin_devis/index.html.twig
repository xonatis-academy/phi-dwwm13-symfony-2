{% extends 'base.html.twig' %}

{% block title %}Devis index{% endblock %}

{% block body %}
    <h1>Devis index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Demande</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Reference</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for devi in devis %}
            <tr>
                <td>{{ devi.id }}</td>
                <td>{{ devi.demande }}</td>
                <td>{{ devi.email }}</td>
                <td>{{ devi.phone }}</td>
                <td>{{ devi.reference }}</td>
                <td>{{ devi.prix }}</td>
                <td>
                    <a href="{{ path('admin_devis_show', {'id': devi.id}) }}">show</a>
                    <a href="{{ path('admin_devis_edit', {'id': devi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_devis_new') }}">Create new</a>
{% endblock %}
