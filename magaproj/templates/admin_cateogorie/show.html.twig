{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categorie.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cateogorie_index') }}">back to list</a>

    <a href="{{ path('admin_cateogorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('admin_cateogorie/_delete_form.html.twig') }}
{% endblock %}
